[package]
name = "vero-audit-guard"
version = "0.1.0"
edition = "2021"
description = "Automated development task for the vero-audit-guard module"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "21.0.0" }
